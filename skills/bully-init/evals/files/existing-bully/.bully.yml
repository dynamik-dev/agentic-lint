schema_version: 1

rules:
  no-todo:
    description: "TODO comments must reference a ticket."
    engine: script
    scope: "*.py"
    severity: warning
    script: "grep -nE 'TODO' {file} | grep -vE '#[0-9]+' && exit 1 || exit 0"
